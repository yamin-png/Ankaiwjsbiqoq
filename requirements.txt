Flask==2.3.2
Flask-Cors==3.1.1  # Cross-origin resource sharing handling
Flask-SQLAlchemy==3.0.2  # SQLAlchemy ORM for database management
Flask-Migrate==4.0.3  # Database migrations for Flask with Alembic
Flask-Login==0.6.2  # User session management
Flask-WTF==1.1.1  # Form handling (WTForms)
Flask-Session==0.4.0  # Server-side session handling
Flask-Mail==0.9.1  # Sending emails in Flask
Flask-Redis==0.4.0  # Redis integration for Flask
Flask-Limiter==2.7.0  # Rate limiting for Flask
Flask-HTTPAuth==4.7.0  # HTTP authentication for Flask APIs
Flask-Testing==0.8.1  # Unit tests for Flask apps

selenium==4.10.0
webdriver-manager==4.8.3  # For automatic management of WebDriver binaries
beautifulsoup4==4.12.2  # Web scraping (used alongside Selenium)
requests==2.31.0  # For making HTTP requests
lxml==4.9.2  # XML and HTML parsing (faster alternative to BeautifulSoup)
pyquery==2.0.0  # jQuery-like library for parsing HTML (alternative to BeautifulSoup)
fake_useragent==1.1.1  # Fake User-Agent generator for Selenium requests

gunicorn==20.1.1  # Production WSGI server for Flask
uvicorn==0.22.0  # Asynchronous server for async frameworks (FastAPI, etc.)
eventlet==0.33.0  # For asynchronous support and scaling Flask

# For asynchronous task queue management
Celery==5.3.0  # Asynchronous task queue management with Flask integration
redis==5.0.0  # Redis server for background tasks (also used for caching)

# Database-related dependencies (for SQL databases like PostgreSQL)
Flask-PostgreSQL==1.0.0  # PostgreSQL support for Flask
psycopg2==2.9.6  # PostgreSQL adapter for Python
SQLAlchemy==2.0.10  # SQLAlchemy ORM
Flask-SQLAlchemy==3.0.2  # Integrating Flask with SQLAlchemy ORM
Flask-Migrate==4.0.3  # Database migrations

# NoSQL database-related dependencies
pymongo==4.6.0  # MongoDB support for Flask
Flask-PyMongo==2.3.0  # MongoDB support for Flask
redis==5.0.0  # Redis integration for caching and storage

# For security
Flask-Talisman==0.8.1  # Security headers for Flask (Content Security Policy, etc.)
Flask-Security==4.0.2  # Authentication and authorization for Flask
Flask-JWT-Extended==4.6.0  # JSON Web Token authentication for Flask APIs
cryptography==39.0.1  # Secure data handling (hashing, encryption, etc.)

# For environment management
python-dotenv==1.0.0  # Manage environment variables in .env files
pyyaml==6.0  # YAML parser for configuration files

# For file handling and storage
Flask-Uploads==0.2.1  # Handling file uploads in Flask
Flask-Cloudy==0.6.0  # Cloud storage for Flask (e.g., Amazon S3, Google Cloud Storage)

# Testing dependencies
pytest==7.7.2  # Testing framework for Flask apps
pytest-cov==4.0.0  # Coverage reports for pytest
tox==3.30.0  # Automation tool for testing in multiple environments

# For logging
Flask-Logging==0.12  # Integrates logging into Flask applications
loguru==0.6.0  # Simplified logging
structlog==21.3.0  # Structured logging
sentry-sdk==2.5.0  # Sentry for application error tracking
newrelic==6.6.0.148  # New Relic for performance monitoring

# For background jobs
RQ==1.11.0  # Redis Queue for Flask background tasks
flower==1.0.0  # Real-time monitoring for RQ jobs

# For DevOps and deployment tools
docker==5.0.0  # Docker support for containerizing the application
docker-compose==1.29.2  # Manage multi-container applications
gunicorn==20.1.1  # WSGI HTTP server for production deployment

# Miscellaneous tools
html5lib==1.1  # HTML parser (alternative to lxml and BeautifulSoup)
PyJWT==2.7.0  # For working with JWT tokens
pycryptodome==3.15.0  # Cryptography (for hashing, encryption)
uwsgi==2.0.21  # Another WSGI HTTP server for production deployment
gevent==22.10.0  # Asynchronous networking support
aiohttp==3.8.1  # Asynchronous HTTP client/server (useful for async apps)
